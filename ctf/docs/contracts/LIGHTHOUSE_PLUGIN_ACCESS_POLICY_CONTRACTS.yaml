policies:
  - pluginId: lighthouse
    command: lighthouse.profile.upsert
    version: 1.0.0
    requiredRoles:
      - member
      - admin
    attributePolicies:
      tenancy: same_workspace
      profileTypeLock: enforced
      profileOwnershipOrAdmin: required
      # Publishing a housing need to the Wanted tab is the member's own explicit choice, stored as
      # is_wanted_public. It defaults false and is never inferred from any other field.
      wantedPostingPublication: opt_in_default_off
      wantedPostingDisclosure: needs_and_preferences_only_no_identity_or_contact
    consentRequirements:
      scopes:
        - lighthouse_profile_management
      fallbackLawfulBasis: explicit_consent_required
    regionRestrictions:
      allowedTransferMechanisms:
        - local_processing
        - scc
    highRiskFlags:
      containsPHI: false
      requiresAdditionalAudit: true
    denyConditions:
      - actor_not_authorized_for_profile
      - invalid_profile_type_transition

  - pluginId: lighthouse
    command: lighthouse.match.request.create
    version: 1.0.0
    requiredRoles:
      - member
    attributePolicies:
      tenancy: same_workspace
      seekerRoleRequired: true
      duplicateRequestPrevention: required
    consentRequirements:
      scopes:
        - lighthouse_matching
      fallbackLawfulBasis: explicit_consent_required
    regionRestrictions:
      allowedTransferMechanisms:
        - local_processing
        - scc
    highRiskFlags:
      containsPHI: false
      requiresAdditionalAudit: true
    denyConditions:
      - actor_not_seeker
      - duplicate_active_or_pending_request
      - blocked_participant_pair

  - pluginId: lighthouse
    command: lighthouse.property.create
    version: 1.0.0
    requiredRoles:
      - member
      - admin
    attributePolicies:
      tenancy: same_workspace
      selfServiceHosting: enabled
      hostProfileAutoProvisioned: true
      profileOwnershipOrAdmin: required
    consentRequirements:
      scopes:
        - lighthouse_property_management
      fallbackLawfulBasis: explicit_consent_required
    regionRestrictions:
      allowedTransferMechanisms:
        - local_processing
        - scc
    highRiskFlags:
      containsPHI: false
      requiresAdditionalAudit: true
    denyConditions:
      - actor_not_authenticated
      - invalid_property_payload
