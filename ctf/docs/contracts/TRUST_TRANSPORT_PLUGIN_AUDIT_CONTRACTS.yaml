auditEvents:
  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.request.create
    commandVersion: 1.0.0
    purpose: marketplace_request_creation
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        roleCheck: pass_or_fail
        modeValidationCheck: pass_or_fail
        regionCheck: pass_or_fail
    dataClassesAccessed:
      - transport_request_metadata
      - route_summary_metadata
    targetContext:
      workspaceId: workspace_identifier
      requestId: request_identifier
      mode: ride_or_package_or_food
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class

  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.offer.create
    commandVersion: 1.0.0
    purpose: marketplace_offer_creation
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        roleCheck: pass_or_fail
        ownershipCheck: pass_or_fail
        requestOpenCheck: pass_or_fail
    dataClassesAccessed:
      - offer_metadata
      - transport_request_metadata
    targetContext:
      workspaceId: workspace_identifier
      requestId: request_identifier
      offerId: offer_identifier
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class

  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.offer.accept
    commandVersion: 1.0.0
    purpose: marketplace_fulfillment_start
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        roleCheck: pass_or_fail
        offerEligibilityCheck: pass_or_fail
        requestOwnershipCheck: pass_or_fail
    dataClassesAccessed:
      - offer_metadata
      - trip_initialization_metadata
    targetContext:
      workspaceId: workspace_identifier
      offerId: offer_identifier
      tripId: trip_identifier
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class

  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.trip.status.update
    commandVersion: 1.0.0
    purpose: lifecycle_transition
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        roleCheck: pass_or_fail
        transitionMatrixCheck: pass_or_fail
        participantAuthorizationCheck: pass_or_fail
    dataClassesAccessed:
      - trip_status_metadata
      - proof_artifact_metadata
    targetContext:
      workspaceId: workspace_identifier
      tripId: trip_identifier
      nextStatus: lifecycle_state
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class

  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.trip.completion.confirm
    commandVersion: 1.0.0
    purpose: lifecycle_transition
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        participantAuthorizationCheck: pass_or_fail
        deliveredStateCheck: pass_or_fail
    dataClassesAccessed:
      - trip_status_metadata
    targetContext:
      workspaceId: workspace_identifier
      tripId: trip_identifier
      bothConfirmed: boolean_flag
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class

  # trust-transport.payout.request audit event was removed (owner decision, 2026-07-08): no platform
  # payout exists for non-ServiceCredits settlement (peer-to-peer, off-platform). See the command contract.

  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.service-credits.transfer
    commandVersion: 1.0.0
    purpose: cross_user_credit_transfer
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        roleCheck: pass_or_fail
        selfTransferCheck: pass_or_fail
        amountValidationCheck: pass_or_fail
    dataClassesAccessed:
      - service_credit_transfer_metadata
    targetContext:
      workspaceId: workspace_identifier
      transferId: transfer_identifier
      recipientId: recipient_identifier
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class

  - eventId: uuid
    timestamp: rfc3339
    actorId: principal
    pluginId: trust-transport
    command: trust-transport.trip.settlement
    commandVersion: 1.0.0
    purpose: trip_settlement_service_credits
    policyDecision:
      status: allow_or_deny
      reason: policy_reason_code
      evidence:
        settlementCurrencyCheck: pass_or_fail
        amountValidationCheck: pass_or_fail
    dataClassesAccessed:
      - service_credit_transfer_metadata
      - trip_status_metadata
    targetContext:
      workspaceId: workspace_identifier
      tripId: trip_identifier
      transferId: transfer_identifier
    requestId: request_identifier
    traceId: trace_identifier
    result:
      status: success_or_failure
      errorCategory: none_or_error_class
