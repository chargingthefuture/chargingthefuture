// Re-export all feed types from the canonical source.
// This file exists so repository.ts can import from 'lib/shared/feed-primitives/types'
// while the actual definitions live in lib/feed/types.
export type {
  FeedRenderMode,
  FeedChannel,
  FeedEnabledChannel,
  FeedQuestionCategory,
  FeedCommunityCategory,
  FeedAnswerRatingValue,
  FeedLocationContext,
  FeedAnswerSource,
  FeedAnswer,
  FeedQuestionDetail,
  FeedCommunityReply,
  FeedCommunityDetail,
  FeedAnnouncementReply,
  FeedAnnouncementDetail,
  FeedQuotedPost,
  FeedReactionSummary,
  PublicCommunityPost,
  FeedPagination,
  FeedTimelineItem,
  FeedConfig,
  FeedConfigInput,
  AnnouncementStatus,
  AnnouncementTargeting,
  Announcement,
  AnnouncementDraftInput,
  MembershipEventType,
  FeedQuestionInput,
  FeedCommunityPostInput,
} from 'lib/feed/types';
