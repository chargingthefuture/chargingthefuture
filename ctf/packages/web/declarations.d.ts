declare module '@ctf/shared/src/mood' {
  /**
   * TODO: Replace this with a concrete interface describing the props and behavior of MoodWeb (currently a React.FC).
   * The actual export is a React functional component, so we use React.FC for now to enable type checking.
   * Update this type when the component's props and shape are finalized.
   */
  import type { FC } from 'react';
  export const MoodWeb: FC;
}
