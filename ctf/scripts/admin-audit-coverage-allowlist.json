{
  "_comment": "Burn-down list for check-admin-audit-coverage.mjs. Owner directive 2026-08-28: every admin action is recorded, on every surface, from the day the surface ships. Every entry here is a surface where that is not yet true — recorded on 2026-08-28 when the gate was introduced, so no NEW surface can ship without an audit row while these are fixed. This list only ever shrinks. Never add an entry to silence the gate; add one only when a durable row is genuinely written somewhere the gate cannot see, and name the file that writes it.",
  "routes": [
    {
      "route": "/api/announcements/admin/targeting/validate",
      "kind": "not-a-mutation",
      "reason": "Validates an announcement targeting expression and returns the normalized form. It reads no table and writes none — a POST only because it takes a body — so there is no admin action here to record. Verified 2026-08-28 against the handler: it calls validateAnnouncementTargeting, a pure function, and returns."
    },
    {
      "route": "/api/mutual-time/events/[eventId]/close",
      "kind": "burn-down",
      "reason": "Mutual Time has no audit machinery at all — no table, no helper, not even a log line. Burn down: add an audit table and write a row on every admin mutation."
    },
    {
      "route": "/api/mutual-time/events",
      "kind": "burn-down",
      "reason": "Mutual Time has no audit machinery at all — no table, no helper, not even a log line. Burn down: add an audit table and write a row on every admin mutation."
    },
    {
      "route": "/api/what-works/admin/problems/[id]",
      "kind": "burn-down",
      "reason": "What Works has lib/what-works/audit.ts, which builds the full contract-shaped event and ends in console.info — a server log line, not a record. Burn down: give it an audit table and route the helper to it."
    },
    {
      "route": "/api/what-works/admin/problems",
      "kind": "burn-down",
      "reason": "What Works has lib/what-works/audit.ts, which builds the full contract-shaped event and ends in console.info — a server log line, not a record. Burn down: give it an audit table and route the helper to it."
    },
    {
      "route": "/api/what-works/admin/products/[id]",
      "kind": "burn-down",
      "reason": "What Works has lib/what-works/audit.ts, which builds the full contract-shaped event and ends in console.info — a server log line, not a record. Burn down: give it an audit table and route the helper to it."
    }
  ]
}
