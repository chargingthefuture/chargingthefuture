{
  "mcpServers": {
    "railway": {
      "url": "https://mcp.railway.com/mcp",
      "headers": {
        "Authorization": "Bearer ${exec:printenv RAILWAY_API_TOKEN}"
      },
      "timeout": 60
    }
  }
}
